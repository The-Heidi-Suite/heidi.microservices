{
  "name": "@heidi/city",
  "version": "1.0.0",
  "description": "City service",
  "scripts": {
    "build": "nest build city",
    "dev": "nest start city --watch",
    "start:dev": "nest start city --watch",
    "start:prod": "node dist/main"
  },
  "dependencies": {
    "@heidi/config": "workspace:*",
    "@heidi/interceptors": "workspace:*",
    "@heidi/logger": "workspace:*",
    "@heidi/metrics": "workspace:*",
    "@heidi/prisma": "workspace:*"
  }
}
