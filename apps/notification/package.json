{
  "name": "@heidi/notification",
  "version": "1.0.0",
  "description": "Notification service",
  "scripts": {
    "build": "nest build notification",
    "dev": "nest start notification --watch",
    "start:dev": "nest start notification --watch",
    "start:prod": "node dist/main"
  },
  "dependencies": {
    "@heidi/prisma": "workspace:*",
    "@heidi/logger": "workspace:*",
    "@heidi/rabbitmq": "workspace:*",
    "@heidi/interceptors": "workspace:*",
    "@heidi/metrics": "workspace:*"
  }
}
